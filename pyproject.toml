[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [".venv", "artifacts", "data/raw", ".model_cache"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
